import { cva } from 'class-variance-authority';

export const inputVariants = cva('w-full outline-0 placeholder:text-neutral-600 text-ellipsis font-medium', {
  variants: {},
  defaultVariants: {},
});
